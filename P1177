#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,x;
    vector<int>arr;
    cin>>n;
    while(n--)
    {
        cin>>x;
        arr.push_back(x);
    }
    sort(arr.begin(),arr.end());
    for(auto ele: arr)
    {
        cout<<ele<<' ';
    }
    return 0;
}
